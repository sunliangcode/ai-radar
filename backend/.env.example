# Core LLM (optional — heuristic mode if empty / placeholder)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Delivery (configure at least one to receive daily push)
FEISHU_WEBHOOK_URL=
WEBHOOK_URL=
SMTP_HOST=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TO=

# Optional
# SMTP_PORT=587
# SMTP_FROM=
# LOCAL_TOKEN=
# GITHUB_TOKEN=
